# new-repo-template
Use this as a template when creating a new repository. Every new repo in the org stumbles into the same setup gaps. This Template should be updated to fill those as they're discovered. 
